/* const chatbotContainer = document.querySelector('.chatbot');
 chatbotContainer.addEventListener('click', () => {
    const userInput = prompt('How can I assist you today?');
    if (userInput) {
        setTimeout(() => {
            alert('Fetching details... Please wait.');
            setTimeout(() => {
                alert('Here are the details. Leave your email for further contact.');
                const email = prompt('Enter your email:');
                if (email) alert('Thank you! We will reach out soon.');
            }, 2000);
        }, 1000);
    }
});*/